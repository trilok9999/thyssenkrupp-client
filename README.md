# thyssenkrupp-client
Ionic Based App for thyssenkrupp app

## Running the app
`grunt serve` if all dependency gems installed. Otherwise `grunt serve --force`

## Running test
`npm test` or `grunt test`
 Using karma and phantomJS
 
## Coverage
`grunt coverage`
